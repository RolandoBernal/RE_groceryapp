<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="content-type" content="text/html; charset=UTF-8">
    <meta charset="utf-8">
    <title><%= user %>'s profile</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css">
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel='stylesheet' href='/stylesheets/style-jrb.css' />
    <link rel='stylesheet' href='/stylesheets/todo.css' />
  </head>
  <body class="body">
    <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
      <div class="navbar-header">
        <a href="" class="navbar-brand">RE Grocery List | <%= user %>'s List</a>
        <a class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        </a>
      </div>
      <div class="navbar-collapse collapse" id="navbar">
        <ul class="nav navbar-nav">
          <li><a href="/grocery">Add Item</a></li>
          <li><a href="/user/profile">View Profile</a></li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
            <li><a href="/user/logout">Log Out</a></li>
        </ul>
      </div>
       </div>
    </nav>

    <div class="container">
    <div class="col-lg-12">
      <p> <br><br> </p>
    </div>



      <div class="col-lg-1 col-md-1 col-sm-1 col-xs-1">
        <p> <br><br> </p>
      </div>
      <div class="col-lg-10 col-md-10 col-sm-10 col-xs-10 mainsection">
        <table class="table">
          <thead>
            <tr>
              <th>Item</th>
              <th>Quantity</th>
              <th>Price</th>
              <th>Delete</th>
              <th>Edit</th>
              <th>Found</th>
            </tr>
          </thead>
          <tbody>
            <% for (var g = 0; g < groceries.length; g++) { %>
            <% var currentItem = groceries[g]; %>
            <tr class="todo-item" id="grocery_<%= currentItem._id %>">
              <td>
                <%= currentItem.item %>
              </td>
              <td>
                <%= currentItem.quantity %>
              </td>
              <td>$
                <%= currentItem.price %>
              </td>
              <td>
                <a class="deleteButton" id="<%= currentItem._id %>">Delete</a>
              </td>
              <td>
                <a class="editButton" id="<%= currentItem._id %>">Edit</a>
              </td>
              <td>
                <a class="completeButton" id="<%= currentItem._id %>">Found</a>
              </td>
            </tr>
            <% } %>
          </tbody>
        </table>
      </div>
      <div class="col-lg-1 col-md-1 col-sm-1 col-xs-1">
        <p> <br><br> </p>
      </div>
    </div>
      <!-- script references -->
  	<script src="http://ajax.googleapis.com/ajax/libs/jquery/2.0.2/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/js/bootstrap.min.js"></script>
    </body>
</html>
